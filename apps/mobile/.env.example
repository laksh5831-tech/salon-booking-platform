# Base URL of the shared Velora backend (web + mobile use the same API).
# For a local emulator / web build, omit to default to http://localhost:5000/api/v1.
# For a real device build, point this at your deployed backend, e.g.:
EXPO_PUBLIC_API_URL=https://velora-api.onrender.com/api/v1